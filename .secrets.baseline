# detect-secrets baseline placeholder
# To generate a real baseline locally:
#   pip install detect-secrets
#   detect-secrets scan > .secrets.baseline
# Review the results, remove false positives, then commit the baseline.
{}
